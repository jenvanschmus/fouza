<style>
  /* 1. Register your custom font from /public/fonts/ */
  @font-face {
    font-family: 'Univern';
    src: url('/fonts/UNIVERN.ttf') format('truetype');
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }

  /* 2. Standard Google Fonts for fallbacks */
  @import url('https://fonts.googleapis.com/css2?family=Architects+Daughter&display=swap');

  :root {
    --header-height: 100px; 
    --logo-scale: 0.8;
  }

  .site-header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: var(--header-height);
    background-color: white;
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.06);
    display: flex;
    align-items: center;
    z-index: 1000;
  }

  .header-container {
    width: 100%;
    padding: 0 4rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .main-logo {
    height: calc(var(--header-height) * var(--logo-scale));
    width: auto;
    display: block;
  }

  .main-nav {
    display: flex;
    align-items: center;
    gap: 3rem;
  }

  .internal-links {
    display: flex;
    gap: 2.5rem;
    /* 3. Apply your custom font here */
    font-family: 'Univern', sans-serif;
    text-transform: uppercase;
    letter-spacing: 0.15em;
  }

  .internal-links a {
    text-decoration: none;
    color: #1a1a1a;
    font-size: 1.1rem; /* Adjusted for custom font readability */
    transition: all 0.3s ease;
  }

  .internal-links a:hover {
    color: #a18a4d; 
    opacity: 0.7;
  }

  .divider {
    width: 1px;
    height: 30px;
    background-color: #f0f0f0;
  }

  .external-links {
    display: flex;
    gap: 1.5rem;
  }

  .external-icon {
    width: 22px;
    height: 22px;
    filter: grayscale(100%);
    opacity: 0.4;
  }
</style>
